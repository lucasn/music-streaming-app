<h1>Playlists</h1>

{% for playlist in playlists %}
    <div class="card-item" onClick="handlePlaylistCardClick({{ playlist.id }})">
        <img src="/images/music.png" alt="music icon">
        <p>{{ playlist.title }}</p>
    </div>
{% endfor %}

<button id="add-playlist-button"><img src="/images/add.png" alt=""></button> 